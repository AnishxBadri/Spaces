node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# runtime data (blobs + secret.key)
data/

docs/*

# stray desktop screenshots dropped at repo root
/Screenshot*.png
