{
  "//": "The root tsconfig covers what did not move: the operator scripts under scripts/ and the two root config files. apps/web has its own (apps/web/tsconfig.json); `pnpm typecheck` runs both, which is why gate 1 is `pnpm typecheck` and not a bare `tsc --noEmit`. eslint's type-aware rules find this file as the nearest tsconfig for scripts/**.",

  "extends": "./packages/config/tsconfig.base.json",

  "include": [
    "scripts/**/*.ts",
    "eslint-rules/**/*.ts",
    "eslint.config.js",
    "prettier.config.js"
  ],
  "exclude": ["node_modules", "apps", "packages"],

  "compilerOptions": {
    "paths": {
      "#/*": ["./apps/web/src/*"]
    }
  }
}
